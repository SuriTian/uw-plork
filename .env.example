# DB_DRIVER=sqlite uses an ephemeral in-memory SQLite DB and ignores the
# DB_HOST/USER/PASSWORD/NAME vars below (used for the free-tier prod deploy).
# Leave unset for local dev — real MySQL, as configured below.
# DB_DRIVER=sqlite

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=uwplork
